---
"@truefoundry/trueforge-ui": patch
---

Remove the redundant composer controls: the adaptive controls input (slash-command palette and active-control chips) and the web-search status indicator no longer wrap the composer input. The underlying server-side control-line handling is unchanged.
