{
  "name": "cart-service",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A tiny checkout service with a real bug in it. Target repository for the Licence to Patch on-call agent.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --import ./instrument.js src/server.js",
    "test": "node --test \"src/**/*.test.js\""
  },
  "dependencies": {
    "@sentry/node": "^9.0.0",
    "express": "^4.21.2"
  }
}
